<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mbc.shk010.login.LoginService">
	<select id="logincheck" resultType="com.mbc.shk010.login.LoginDTO">
		select id, pw from member0906 where id=#{param1} and pw=#{param2}
	</select>
	
	
</mapper>
